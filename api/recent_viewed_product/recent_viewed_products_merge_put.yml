- api:
    def: api_recent_viewed_products_merge_PUT()
    request:
      url: /api/recent_viewed_products/merge
      method: PUT
      headers:
        x-channel: web
        x-guest-token: ${ENV(x_guest_token)}
        x-access-token: $access_token
        user-agent: ${ENV(user_agent_guest)}